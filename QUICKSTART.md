# Quick Start Guide

Быстрое руководство по запуску VPN сервиса за 5 минут.

## Шаг 1: Подготовка (2 минуты)

```bash
# Клонируйте репозиторий
git clone <your-repo-url>
cd vpn-service

# Сгенерируйте Xray ключи
make generate-keys
```

Скопируйте **Private key** из вывода команды.

## Шаг 2: Конфигурация (1 минута)

```bash
# Создайте .env файл
cp .env.example .env

# Отредактируйте .env
nano .env
```

Установите:
```bash
XRAY_PRIVATE_KEY=<ваш_приватный_ключ_из_шага_1>
SERVER_IP=<ваш_публичный_IP>
```

Сохраните файл (Ctrl+O, Enter, Ctrl+X).

## Шаг 3: Запуск (2 минуты)

```bash
# Сборка
make build

# Запуск всех сервисов
make up

# Проверка статуса
make status
```

Все сервисы должны быть в статусе "Up".

## Шаг 4: Создание пользователя (30 секунд)

```bash
# Создать пользователя
curl -X POST http://localhost:8080/api/users \
  -H "Content-Type: application/json" \
  -d '{
    "username": "myuser",
    "password": "mypassword123",
    "traffic_limit": 10737418240,
    "expires_at": "2025-12-31T23:59:59Z"
  }'
```

Или используйте Makefile:
```bash
make create-user
```

## Шаг 5: Получение конфигурации (30 секунд)

```bash
# Получить ID пользователя
make list-users

# Получить конфигурацию (замените 1 на ваш ID)
make get-user-config USER_ID=1
```

В ответе вы получите:
- ✅ JSON конфигурацию
- ✅ VLESS URI (для импорта)
- ✅ QR код (base64)

## Готово! 🎉

Ваш VPN сервис запущен и готов к использованию.

### Что дальше?

#### Импорт в клиент

1. Скопируйте **VLESS URI** из ответа
2. Откройте VPN клиент (v2rayNG, Nekoray, Shadowrocket)
3. Добавьте новое подключение из URI или QR кода
4. Подключитесь!

#### Доступные сервисы

- **API**: http://localhost:8080
- **Prometheus**: http://localhost:9090
- **Grafana**: http://localhost:3000 (admin/admin)

#### Полезные команды

```bash
# Просмотр логов
make logs

# Список пользователей
make list-users

# Статистика сервиса
make test-stats

# Остановка
make down

# Полная очистка
make clean
```

## Troubleshooting

### Порт 443 занят
```bash
sudo lsof -i :443
# Остановите сервис, использующий порт
```

### Ошибка при сборке
```bash
# Пересоберите без кэша
make rebuild
```

### Не могу подключиться
1. Проверьте firewall: `sudo ufw status`
2. Убедитесь что порт 443 открыт
3. Проверьте что пользователь активен: `make list-users`

## Дополнительная информация

- [Полная документация](README.md)
- [Руководство по деплою](DEPLOYMENT.md)
- [Примеры использования API](examples/)

## Поддержка

Нужна помощь? 
- Проверьте логи: `make logs`
- Читайте документацию: [README.md](README.md)
- Откройте issue на GitHub

