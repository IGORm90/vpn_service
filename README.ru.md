# VPN Service с VLESS протоколом

Полнофункциональный VPN сервис на базе Xray-core с управлением пользователями, мониторингом трафика и метриками.

## 🏗️ Архитектура

- **Go Application** - встроенный Xray-core, REST API, управление пользователями
- **SQLite** - база данных пользователей
- **Prometheus** - сбор метрик
- **Grafana** - визуализация метрик
- **VLESS + Reality + xHTTP** - современный защищенный VPN протокол

## 📋 Возможности

- ✅ Управление пользователями через REST API
- ✅ Лимиты трафика и срок действия подписки
- ✅ Автоматическое отслеживание использованного трафика
- ✅ Генерация клиентских конфигураций (JSON, URI, QR-код)
- ✅ Мониторинг в реальном времени (Prometheus + Grafana)
- ✅ VLESS протокол с Reality и xHTTP
- ✅ База данных SQLite для хранения пользователей

## 🚀 Быстрый старт

### 1. Предварительные требования

- Docker и Docker Compose
- make (опционально, для удобства)

### 2. Настройка

```bash
# Клонируйте репозиторий
git clone <repo-url>
cd vpn-service

# Сгенерируйте ключи и создайте .env
make setup

# Отредактируйте .env файл
nano .env
```

Обязательно установите:
- `XRAY_PRIVATE_KEY` - приватный ключ из `make generate-keys`
- `SERVER_IP` - IP адрес вашего сервера

### 3. Запуск

```bash
# Сборка и запуск
make build
make up

# Проверка статуса
make status

# Просмотр логов
make logs
```

### 4. Создание первого пользователя

```bash
# Через Makefile
make create-user

# Или напрямую через curl
curl -X POST http://localhost:8080/api/users \
  -H "Content-Type: application/json" \
  -d '{
    "username": "john",
    "password": "secret123",
    "traffic_limit": 10737418240,
    "expires_at": "2025-12-31T23:59:59Z"
  }'
```

## 📡 API Endpoints

### Пользователи

#### Создать пользователя
```bash
POST /api/users
Content-Type: application/json

{
  "username": "john_doe",
  "password": "securepass123",
  "traffic_limit": 10737418240,  // 10GB в байтах, 0 = безлимит
  "expires_at": "2025-12-31T23:59:59Z"  // опционально
}
```

#### Список пользователей
```bash
GET /api/users
GET /api/users?active=true  // только активные
```

#### Получить пользователя
```bash
GET /api/users/{id}
```

#### Обновить пользователя
```bash
PATCH /api/users/{id}
Content-Type: application/json

{
  "traffic_limit": 21474836480,  // 20GB
  "is_active": true
}
```

#### Удалить пользователя
```bash
DELETE /api/users/{id}
```

#### Получить конфигурацию клиента
```bash
GET /api/users/{id}/config
```

Возвращает:
- JSON конфигурацию для v2rayN, Nekoray
- VLESS URI
- QR-код (base64)
- Статистику трафика

#### Сбросить трафик
```bash
POST /api/users/{id}/reset-traffic
```

### Системные

#### Health Check
```bash
GET /health
```

#### Статистика
```bash
GET /stats
```

#### Prometheus метрики
```bash
GET /metrics
```

## 📊 Мониторинг

### Grafana

1. Откройте http://localhost:3000 (или `make open-grafana`)
2. Логин: `admin` / Пароль: `admin`
3. Дашборд: VPN Service Metrics

### Prometheus

Метрики доступны на http://localhost:9090

Доступные метрики:
- `vpn_active_users_total` - количество активных пользователей
- `vpn_total_users` - всего пользователей
- `vpn_expired_users` - с истекшим сроком
- `vpn_users_over_limit` - превысили лимит
- `vpn_user_traffic_bytes` - трафик по пользователям
- `vpn_connections_total` - всего подключений
- `vpn_connections_active` - активных подключений

## 🔧 Makefile команды

### Основные
```bash
make help          # Показать помощь
make setup         # Первоначальная настройка
make up            # Запустить сервисы
make down          # Остановить сервисы
make restart       # Перезапустить
make logs          # Просмотр логов
make status        # Статус сервисов
make clean         # Удалить все данные
```

### Управление пользователями
```bash
make create-user                    # Создать тестового пользователя
make list-users                     # Список пользователей
make get-user-config USER_ID=1      # Получить конфигурацию
make delete-user USER_ID=1          # Удалить пользователя
```

### Тестирование
```bash
make test-health   # Проверить health
make test-stats    # Проверить статистику
make metrics       # Показать метрики
```

### Резервное копирование
```bash
make db-backup     # Создать backup БД
make db-restore    # Восстановить БД
```

## 📁 Структура проекта

```
vpn-service/
├── app/
│   ├── main.go              # Точка входа
│   ├── database/            # База данных
│   │   ├── models.go        # Модели
│   │   ├── database.go      # Подключение
│   │   └── repository.go    # CRUD операции
│   ├── xray/                # Интеграция Xray
│   │   ├── manager.go       # Управление Xray
│   │   ├── config.go        # Генерация конфигурации
│   │   └── client_config.go # Клиентские конфиги
│   ├── api/                 # REST API
│   │   ├── handlers.go      # Обработчики
│   │   ├── middleware.go    # Middleware
│   │   └── responses.go     # Ответы
│   ├── monitoring/          # Мониторинг
│   │   ├── log_parser.go    # Парсер логов
│   │   └── metrics.go       # Метрики
│   └── utils/               # Утилиты
│       ├── crypto.go        # Криптография
│       └── qrcode.go        # QR-коды
├── prometheus/
│   └── prometheus.yml       # Конфигурация Prometheus
├── grafana/
│   ├── datasources/         # Источники данных
│   └── dashboards/          # Дашборды
├── docker-compose.yml       # Docker Compose
├── Makefile                 # Автоматизация
└── README.ru.md            # Документация

```

## 🔐 Безопасность

1. **Измените пароли по умолчанию** в docker-compose.yml (Grafana)
2. **Сгенерируйте уникальные ключи** для Xray Reality
3. **Используйте HTTPS** для API в production (добавьте reverse proxy)
4. **Настройте firewall** для ограничения портов
5. **Регулярно обновляйте** зависимости и Docker образы

## 🔑 Модель пользователя

```go
type User struct {
    ID           uint      // Уникальный ID
    Username     string    // Уникальное имя пользователя
    Password     string    // Хэш пароля (bcrypt)
    UUID         string    // UUID для VLESS
    Secret       string    // Secret для Shadowsocks (резерв)
    IsActive     bool      // Активен ли пользователь
    ExpiresAt    time.Time // Срок истечения подписки
    TrafficLimit int64     // Лимит трафика в байтах (0 = безлимит)
    TrafficUsed  int64     // Использовано трафика
    CreatedAt    time.Time // Дата создания
    UpdatedAt    time.Time // Дата обновления
}
```

## 📱 Клиентские приложения

### Android
- v2rayNG
- Nekoray

### iOS
- Shadowrocket
- V2Box

### Windows/macOS/Linux
- v2rayN / v2rayNG
- Nekoray
- Qv2ray

### Подключение

1. Получите конфигурацию: `GET /api/users/{id}/config`
2. Используйте QR-код или VLESS URI
3. Импортируйте в клиентское приложение

## 🐛 Устранение неполадок

### Xray не запускается

```bash
# Проверьте логи
make logs-go

# Убедитесь что XRAY_PRIVATE_KEY установлен
docker exec vpn-app env | grep XRAY_PRIVATE_KEY

# Проверьте что порт 443 свободен
sudo lsof -i :443
```

### База данных недоступна

```bash
# Проверьте volume
docker volume inspect vpn-serice_vpn-data

# Пересоздайте контейнер
make down
make up
```

### Пользователи не могут подключиться

1. Убедитесь что пользователь активен: `GET /api/users/{id}`
2. Проверьте что не истек срок и не превышен лимит
3. Убедитесь что VLESS порт 443 доступен извне
4. Проверьте firewall правила

## 🔄 Обновление

```bash
# Остановите сервисы
make down

# Обновите код
git pull

# Пересоберите
make rebuild

# Запустите
make up
```

## 📝 TODO / Roadmap

- [ ] Web UI для управления
- [ ] Поддержка Shadowsocks протокола
- [ ] Telegram бот для управления
- [ ] Автоматическая ротация ключей
- [ ] Уведомления о превышении лимитов
- [ ] Реферальная система
- [ ] Биллинг интеграция

## 🤝 Вклад в проект

Приветствуются pull requests и issues!

## 📄 Лицензия

MIT License

## 💡 Полезные ссылки

- [Xray-core документация](https://xtls.github.io/)
- [VLESS протокол](https://github.com/XTLS/Xray-core)
- [Reality протокол](https://github.com/XTLS/REALITY)

---

**Приятного использования! 🚀**

